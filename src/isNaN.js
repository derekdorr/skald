/**
 * Determine if value is NaN
 *
 * @func
 * @since 1.0.0
 * @param {number}
 * @return {boolean}
 * @example
 *
 *     isNaN(NaN); //=> true
 *     isNaN(1); //=> false
 */

const { isNaN } = Number;

export default isNaN;
