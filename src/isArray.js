/**
 * Determine if value is array
 * 
 * @func
 * @since 1.0.0
 * @param {*}
 * @return {boolean}
 * @example
 * 
 *     isArray([]); //=> true
 *     isArray(1); //=> false
 */

const { isArray } = Array;

export default isArray;
