/**
 * Determine if value is array
 *
 * @module isArray
 * @func
 * @since 1.0.0
 * @param {*} val
 * @return {boolean}
 * @example
 *
 *     isArray([]); //=> true
 *     isArray(1); //=> false
 */

import _isArray from './_internal/_isArray';

export default _isArray;
