/**
 * A function which returns whatever is passed into it
 *
 * @module identity
 * @func
 * @since 1.5.0
 * @params {*} val
 * @return {*}
 * @example
 *
 *     identity(5); //=> 5
 *     identity({}); //=> {}
 */

const identity = val => val;

export default identity;
