/**
 * Returns false if truthy, true if falsy
 *
 * @module not
 * @func
 * @since 1.1.0
 * @param {*} val
 * @return {boolean}
 * @example
 *
 *     not(1); //=> false
 *     not(false); //=> true
 */

const not = val => !val;

export default not;
